import styled from 'styled-components';

const H2 = styled.h2`
  font-size: 1.5em;
  margin: 0px;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  width: 400px;
`;

export default H2;
