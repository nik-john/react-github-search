/*
 * SortFilter Messages
 *
 * This contains all the text for the SortFilter component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  label: {
    id: 'gh-search.components.SortFilter.label',
    defaultMessage: 'Sort By',
  },
});
