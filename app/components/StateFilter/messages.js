/*
 * DateFilter Messages
 *
 * This contains all the text for the DateFilter component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  label: {
    id: 'gh-search.components.DateFilter.label',
    defaultMessage: 'Since',
  },
});
