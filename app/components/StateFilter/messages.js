/*
 * StateFilter Messages
 *
 * This contains all the text for the StateFilter component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  label: {
    id: 'gh-search.components.StateFilter.label',
    defaultMessage: 'State',
  },
});
